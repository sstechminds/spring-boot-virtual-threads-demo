name: CodeQL - Maven

on:
  push:
    branches: [ main ]
#  schedule:
#    - cron: '6 11 * * SUN'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  codeql:
    name: codeql
    uses: sstechminds/spring-boot-virtual-threads-demo/.github/workflows/codeql-analysis.yml
    with:
      java: 25
      pom: pom.xml
    secrets: inherit
