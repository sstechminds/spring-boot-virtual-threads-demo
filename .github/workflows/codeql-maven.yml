name: CodeQL - Maven

on:
  schedule:
    - cron: '6 11 * * SUN'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  codeql:
    name: codeql
    uses: ./.github/workflows/codeql-maven.yml
    with:
      java: 25
      pom: pom.xml
    secrets: inherit
